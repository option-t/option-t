const ERR_MSG_MUST_NOT_RETURN = ' must not return .ts';
const DEFAULT_ARG_NAME = '`def`.ts';

export const ERR_MSG_SELECTOR_MUST_NOT_RETURN = '`selector`' + ERR_MSG_MUST_NOT_RETURN;
export const ERR_MSG_CALLED_WITH = 'called with .ts';
export const ERR_MSG_DEF_MUST_NOT_BE = DEFAULT_ARG_NAME + ' must not be .ts';
export const ERR_MSG_DEF_MUST_NOT_RETURN = DEFAULT_ARG_NAME + ERR_MSG_MUST_NOT_RETURN;

name: CI on main branch

on:
    push:
        branches:
            - "main"

permissions: {}

jobs:
    ci:
        uses: ./.github/workflows/_ci.yaml
        permissions:
            contents: read
